enum ClickAction {
  signInWithGoogle,
  signInWithApple,
  signInWithEmailVerificationLink
}

// ignore: constant_identifier_names
enum Gender { Male, Female, NonBinary, Unsure, RatherNotSay }

enum AppointmentStatus { scheduled, accepted, rejected, cancelled }
